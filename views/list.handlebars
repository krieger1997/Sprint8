<h1>Lista de participantes</h1>

<table class="table table-dark table-striped">
    <thead>
        <tr>
            <th>#</th>
            <th>Foto</th>
            <th>Nombre</th>
            <th>Años Experiencia</th>
            <th>Especialidad</th>
            <th>Estado</th>
        </tr>
    </thead>
    <tbody>
    {{#each skaters}}
        <tr>
            <td>{{@index}}</td>
            <td> <img width="100" src="/images/{{this.skaterId}}" alt="Imagen skater {{@index}}" ></td>
            <td>{{this.nombre}}</td>
            <td>{{this.anosExperiencia}}</td>
            <td>{{this.especialidad}}</td>
            <td>{{this.estado}}</td>
        </tr>    
    {{/each}}
    </tbody>
</table>
<pre>
{{json skaters }}

</pre>